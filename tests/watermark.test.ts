import { queryEndpoint } from "./libs/utils";


describe("Test Watermarking", () => {
	
	test('two plus two is four', () => {
		expect(2 + 2).toBe(4);
	});

	test('two plus two is four', () => {
		expect(2 + 2).toBe(4);
	});

});